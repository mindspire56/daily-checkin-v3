# 版本记录

> **当前版本：3.5.3.0**（手机适配 + 阅读内置书库 + 工具栏扩展 + 专注自定义时长 + 背单词专区）。
> 保留的历史快照：`versions/3.2.2.9`（最后一版含社交功能的稳定版）、`versions/3.5.1.0`、`versions/3.5.2.0`、`versions/3.5.3.0`。
> 「联网社交版（大飞跃）」在独立分支 **`social-dev`** 开发，不影响 main；该方向因暂无云服务暂搁置。

---

## 3.5.3.0（手机端适配 + 阅读书库 + 工具栏扩展 + 专注自定义 + 背单词专区；当前版）

**1. 手机端全面适配**
- 学科三学段（小学 / 初中 / 高中）由横向排布改为**竖向堆叠**（`flex-direction:column`），学段卡片整宽、年级 chip 左对齐，不再过宽。
- 工具横条 `tool-strip` 改 `grid` 两列等宽，避免横向拥挤。
- 专注 chip、flash-card、recite-chip、quiz-q、reader-body 等加 `flex-wrap` / 收紧边距；新增 `@media (max-width:520px)` 细节收紧（工具项、专注时钟、卡片、背诵 chip `flex:1 0 30%` 三列）。
- 星火页 `max-width:560px;margin:0 auto`，移动端满宽、桌面端居中，整体「像 App 不像网页」。

**2. 阅读内置书库（全文入库）**
- 新增 8 个分类 chip：全部 / 古诗 / 现代诗 / 历史 / 天文 / 地理 / 科学 / 医学。
- 种子书 7 本，内容**全文入库**（`SEED_BOOKS`）：古诗精选（15 首）、现代诗选（6 首）、中国历史常识（4 节）、天文趣谈（3 节）、地理知识（3 节）、科学启蒙（3 节）、医学常识（3 节，科普向、提示「先存放、之后可添加自己的书」）。
- `seedBooksIfEmpty()` 仅首次播种；书架筛选按「书架 × 分类」二维过滤，分类可独立切换。
- 用户仍可用「＋ 导入电子书」导入自己的 .txt / .md / .epub。

**3. 工具栏扩展（5 → 11）**
- 原工具箱首页 5 项扩展为 11 项：新增 🎲骰子/随机、🔀随机抽人、📝速记便签（存 `DB.ns('notes')`）、💡闪卡速记、🔤单词卡（用 `ENG_BANK`）、🫧呼吸放松。
- 工具以**自包含弹层**形式实现（`openToolModal` 注入 `#toolModalMask`，不依赖 `window.Tools`），含深色主题适配。
- 全屏工具（白噪音 / 常亮等）保持 ≥7 项。

**4. 专注自定义时长**
- 固定 25 / 45 / 5 三档之外，新增**「自定义」入口**：输入 1–180 分钟，逻辑经 `focusDurFor(m)` 统一换算秒数；`setFocusMode` 与完成计数均支持 `custom`。

**5. 背单词独立专区**
- 在「练习」内独立成区（`#practiceWords`）：分学段（小学 / 初中 / 高中）+ **分年级**（一年级…）。
- 种子词库 `SEED_WORDS`：小学 14 / 初中 12 / 高中 12，共 38 词；支持**自定义添加不会的单词**（`wordEditor`，持久化到 `DB.ns('wordbank')`）。
- 单词来源三合一：`getWordPool()` 合并 种子词（按学段）+ 用户生词（按学段+年级）+ **学科教材库里 `subject==='英语'` 的条目**（与学科英语/教材库打通）。
- 背记模式：翻卡（flip）/ 默写听写（dictation），支持「认识 / 不会」分流与退出。

**质量**
- jsdom 冒烟测试 **25 项全通过、0 JS 运行时错误**（`devlog/3.5.3.0/smoke.js`）。
- 6 个 `<script>` 块逐块语法校验通过；全文 0 个不可见控制字符、0 个 U+FFFD 乱码。
- **修复**：`renderBookList` 的 `filter` 回调此前漏写 `return`，导致 7 本种子书被全部过滤掉（书架显示空白）——已补 `return` 修复（详见 §7 坑13）。
- 安卓：versionCode 10 → 11，versionName 3.5.3.0；APK 见 `发布包/星火每日打卡-3.5.3.0.apk`。

---

## 3.5.2.0（星火空间重设计 + 练习实践化 + 背诵扩容）

**导览行（回退用户偏好）**
- 3.5.1.0 曾把 ✨星火 做成正式导览项 —— **本版回退为悬浮按钮**。
- 现为：今日 / 阅读 / **✨星火（悬浮 FAB）** / 练习 / 休闲。悬浮钮激活时带辉光，按压 `scale(.92)`（去掉了旋转）。

**首页「换一个」还原**
- 恢复旧版排版：**单行不换行**（`white-space:nowrap`）、小图标（10px、`opacity:.7`）、浅色（`#b8a69d`）。

**星火空间：预览 + 点击两层结构**
- 首页四张卡，内容一眼可见，不需要点进去才知道有什么：
  1. 🧰 **多功能工具箱** — 工具横条直接铺开（回归旧版"需要啥一眼看见"）。
  2. ⏱️ **专注模式** — 首页直接显示 25:00 时钟 + 时长 chip + 开始/重置（回归旧版专注模式手感），另有「全屏」入口。
  3. 📚 **学科学习** — 小学 / 初中 / 高中三张卡，**年级 chip 直接铺在旁边**。
  4. ✨ **背诵** — 六大类 chip 直接可见可点。
- 学科三级流转：**年级 chip → 学科分类（带面包屑 + 条目数）→ 该科目内容空间**。
  - 学段学科按学段区分（小学 5 科 / 初中 9 科 / 高中 9 科），每科带图标。
  - 内容空间保留「知识点预留区」提示，架构已就位，后续按年级学科灌入知识点。
  - 数据字段扩为 `{level, grade, subject, title, content}`。
- **返回键灵敏度优化**：吸顶（`position:sticky`）、最小热区 44px、`touch-action:manipulation`（消除 300ms 点击延迟）、`-webkit-tap-highlight-color:transparent`、按压反馈 `scale(.96)` + 高亮底色。

**练习：把学会的东西拿来实践**
- 新增 **4 种练习来源**（选择后持久化到 `DB.ns('practiceCfg')`）：
  1. 🗂️ **我的卡片** — 全部背诵卡片
  2. 📚 **学科练习** — 学科学习里录入的知识点直接出题（标题为题面，内容为答案）
  3. 🔤 **英语单词** — 内置 20 词词库 + 自己的英语类卡片
  4. 🎯 **自选练习** — 勾选清单，**学了什么就练什么**；清单自动聚合各牌组 + 各「年级·学科」+ 内置词库，带条目数
- 新增 **默写听写** 模式：只放发音不显示题面，`speechSynthesis` 朗读（英文 en-US / 中文 zh-CN，语速 0.88），可重复播放、可「想不起来看答案」。
- 练习方式扩为：选择题 / 填空题 / **默写听写** / 随机（随机在三者中混合）。
- 每种方式带一句说明文案，降低选择成本。
- 新增 **今日练习统计**：可练题量 / 今日已练 / 今日正确率（`DB.ns('practiceLog')`，跨天自动清零）。
- 填空判定放宽：忽略大小写与多余空格，长答案支持前缀容错。

**背诵：「背」和「诵」的结合**
- 六大分类全部落地：**课文背诵 / 句子背诵 / 英语单词 / 英语句子朗读 / 中文古诗词 / 现代诗朗诵**。
- 内容扩容：种子卡片 **26 → 68 条**，六个新分类每类 8–10 条（《陋室铭》《爱莲说》《出师表》《桃花源记》《少年中国说》《将进酒》《水调歌头》《竹石》《木兰诗》《再别康桥》《乡愁》《雨巷》《致橡树》《相信未来》等）。
- 新增「**诵**」：抽查模式加「🔊 朗读一遍」，正常背诵加「🔊 朗读 / 🐢 慢速」；英语类读英文原句，中文类读全文。
- 背诵页顶部加引导语：「背 · 记住它 ｜ 诵 · 读出来」。

**质量**
- jsdom 冒烟测试 **93 项全通过、0 JS 运行时错误**；epub 专项 9 项全通过。
- 6 个 `<script>` 块逐块语法校验通过；全文 0 个不可见控制字符、0 个 U+FFFD 乱码。
- 安卓：versionCode 9 → 10，versionName 3.5.2.0。

---

## 3.5.1.0（导览重构 + 阅读模块 + 星火空间）

**版本隔离**
- 存储键 `daily-checkin-v30` → **`daily-checkin-v35`**（主题键、引导键、工具箱键同步迁移；与 3.2.2.9 数据完全隔离，互不覆盖）。
- 引导键 `daily-checkin-guide-3.0.0` → `daily-checkin-guide-3.5.0`。
- 安卓：同包名 `com.dailycheckin.v3`，显示名 **「星火 · 学习」**（区别于旧版「星火」），versionCode 8 → 9，versionName 3.5.1.0。

**导览行**
- 旧：今日 / 学习 / 花火(悬浮+) / 练习 / 休闲
- 新：**今日 / 阅读 / 星火 / 练习 / 休闲**（星火转为正式导览项，悬浮 `+` 按钮移除）

**阅读（新增）**
- 支持导入 **epub（原生解析）** 与 **txt / md**。
  - epub：手写 ZIP 中央目录解析 + `DecompressionStream('deflate-raw')` 解压，读 `container.xml` → `content.opf` → spine 章节顺序；XHTML 经 `sanitizeHtml` 剥离 `<script>`/`<style>`/`on*` 事件属性。
  - txt/md：按空行与章节标题切分章节。
- 书架分组（在读 / 想读 / 读完）、阅读进度、目录跳转、上一章 / 下一章。
- **书签**、**划线**、**读书笔记**（按书归档，`DB.ns('reading')`）。

**星火（原花火改名，学习模式二合一）**
四个分区：
1. **专注模式** — 番茄钟（25 / 45 / 5 分钟档），环形进度、暂停 / 重置、完成计数。
2. **工具栏** — 复用既有工具箱 + 新增计算器 / 白噪音 / 屏幕常亮（`navigator.wakeLock`，安卓走 `AndroidBridge.keepScreenOn`），预留扩展位。
3. **学科学习** — 小学 / 初中 / 高中 → 10 个学科 → 「年级 + 学科」专属资料空间，资料增删改查（独立数据域 `DB.ns('subjects')`，与背诵卡片互不干扰）。
4. **背诵** — 三模式：
   - **自定义背诵**：牌组 / 正面 / 背面 / 提示 / 例句，完整增删改。
   - **抽查式背诵**：Leitner 间隔重复翻转卡（BOX_DAYS `[0,1,3,7,16]`）。
   - **正常背诵**：随机推荐常识 / 古诗 / 名言，纯浏览、**不计分不测验**。

**练习**
- 自建测验，答错自动回流星火错题（`DB.ns('wrong')`）。

**返回栈**
- `computeCanBack` / `appBack` 面板键修正为 `['reading','spark','practice','leisure']`（旧键 `study`/`recite` 会导致 `panels[k]` 为 `undefined` 崩溃）。
- 新增 `window.__subBack()`：星火子页 / 阅读器 / 学科空间 / 卡片编辑器**逐级返回**，不再一步退到今日。
- 修正 `window.__activate` 的定义位置（此前在星火模块引用了不存在的 `activate`，会抛 ReferenceError）。

**质量校验**
- `devlog/3.5.1.0/smoke.js`：jsdom 全流程 60 项断言，**全部 PASS，0 JS 错误**。
- `devlog/3.5.1.0/smoke_epub.js`：手工构造真实 epub（ZIP + deflate）导入验证 9 项，**全部 PASS**，含 XSS sanitize 校验。

---

## 3.5.0.0（去社交 + 火花背诵雏形）
- 切除全部社交 / 好友 / 朋友空间功能；新增「火花」Leitner 背诵（23 张种子卡）与「练习」自测。
- 已部署线上，后被 3.5.1.0 取代。

## 3.2.2.9（最后一版含社交功能的稳定版）
- **问题**：深色模式下「编辑资料」抽屉与聊天页仍为浅色。
- **修复**：补充 `[data-theme="dark"]` 下 `.profile-drawer`、`.chat-view`、`.chat-body`、`.msg-row .m-bubble` 等深色规则。
- 快照保留于 `versions/3.2.2.9/index.html`。
